<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Presentation</title>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

```
:root {
  --cream: #f5f0e8;
  --dark: #1a1a1a;
  --accent: #c8a96e;
  --muted: #7a7a72;
}

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--cream);
  color: var(--dark);
  min-height: 100vh;
}

/* ── LOCK SCREEN ── */
#lock {
  position: fixed; inset: 0;
  background: var(--dark);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2rem;
  z-index: 100;
  transition: opacity 0.6s ease;
}

#lock.hidden {
  opacity: 0;
  pointer-events: none;
}

.lock-title {
  font-family: 'DM Serif Display', serif;
  font-size: clamp(1.8rem, 5vw, 2.8rem);
  color: var(--cream);
  letter-spacing: -0.02em;
  text-align: center;
}

.lock-sub {
  font-size: 0.85rem;
  color: var(--muted);
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.lock-form {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
  width: 100%;
  max-width: 320px;
  padding: 0 1.5rem;
}

.lock-input {
  width: 100%;
  padding: 0.9rem 1.2rem;
  background: transparent;
  border: 1px solid #3a3a3a;
  border-radius: 4px;
  color: var(--cream);
  font-family: 'DM Sans', sans-serif;
  font-size: 1rem;
  letter-spacing: 0.05em;
  outline: none;
  transition: border-color 0.2s;
  text-align: center;
}

.lock-input:focus { border-color: var(--accent); }
.lock-input::placeholder { color: #555; }

.lock-btn {
  width: 100%;
  padding: 0.9rem;
  background: var(--accent);
  border: none;
  border-radius: 4px;
  color: var(--dark);
  font-family: 'DM Sans', sans-serif;
  font-size: 0.9rem;
  font-weight: 500;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  cursor: pointer;
  transition: opacity 0.2s;
}

.lock-btn:hover { opacity: 0.85; }

.lock-error {
  font-size: 0.8rem;
  color: #e07070;
  letter-spacing: 0.05em;
  min-height: 1rem;
}

/* ── CONTENT ── */
#content {
  display: none;
  min-height: 100vh;
}

#content.visible { display: block; }

header {
  padding: 3rem 2rem 2rem;
  border-bottom: 1px solid #ddd8cc;
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  max-width: 900px;
  margin: 0 auto;
}

.header-label {
  font-size: 0.72rem;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--muted);
}

.header-title {
  font-family: 'DM Serif Display', serif;
  font-size: clamp(2rem, 6vw, 3.5rem);
  line-height: 1.1;
  letter-spacing: -0.02em;
  margin-top: 0.4rem;
}

.header-date {
  font-size: 0.8rem;
  color: var(--muted);
  text-align: right;
}

main {
  max-width: 900px;
  margin: 0 auto;
  padding: 3rem 2rem;
}

.section {
  margin-bottom: 3rem;
  opacity: 0;
  transform: translateY(16px);
  animation: fadeUp 0.5s ease forwards;
}

.section:nth-child(1) { animation-delay: 0.1s; }
.section:nth-child(2) { animation-delay: 0.2s; }
.section:nth-child(3) { animation-delay: 0.3s; }

@keyframes fadeUp {
  to { opacity: 1; transform: translateY(0); }
}

.section-label {
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 1rem;
}

.section-heading {
  font-family: 'DM Serif Display', serif;
  font-size: 1.5rem;
  margin-bottom: 0.75rem;
}

.section-body {
  font-size: 0.95rem;
  line-height: 1.75;
  color: #444;
  max-width: 620px;
}

.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1rem;
  margin-top: 1.25rem;
}

.card {
  background: #fff;
  border: 1px solid #e8e3d8;
  border-radius: 6px;
  padding: 1.25rem 1.5rem;
}

.card-num {
  font-family: 'DM Serif Display', serif;
  font-size: 2rem;
  color: var(--accent);
  line-height: 1;
}

.card-label {
  font-size: 0.8rem;
  color: var(--muted);
  margin-top: 0.25rem;
}

footer {
  max-width: 900px;
  margin: 0 auto;
  padding: 2rem;
  border-top: 1px solid #ddd8cc;
  font-size: 0.75rem;
  color: var(--muted);
  letter-spacing: 0.05em;
}
```

  </style>
</head>
<body>

  <!-- LOCK SCREEN -->

  <div id="lock">
    <div>
      <p class="lock-sub">Protected Presentation</p>
      <h1 class="lock-title">Enter to<br/><em>Continue</em></h1>
    </div>
    <div class="lock-form">
      <input id="pw" class="lock-input" type="password" placeholder="Password" />
      <button class="lock-btn" onclick="checkPassword()">Enter</button>
      <p class="lock-error" id="err"></p>
    </div>
  </div>

  <!-- CONTENT -->

  <div id="content">
    <header>
      <div>
        <p class="header-label">Sample Presentation</p>
        <h1 class="header-title">This Is<br/>A Test Page</h1>
      </div>
      <p class="header-date">March 2026</p>
    </header>

```
<main>
  <div class="section">
    <p class="section-label">Overview</p>
    <h2 class="section-heading">Everything is working</h2>
    <p class="section-body">If you're reading this, the password worked and your GitHub Pages setup is live. This page is hosted at your permanent URL and can be embedded into ClickUp anytime.</p>
  </div>

  <div class="section">
    <p class="section-label">What's Next</p>
    <h2 class="section-heading">Ready for real content</h2>
    <div class="card-grid">
      <div class="card">
        <div class="card-num">01</div>
        <div class="card-label">Build a real presentation with Claude</div>
      </div>
      <div class="card">
        <div class="card-num">02</div>
        <div class="card-label">Upload to GitHub and share the URL</div>
      </div>
      <div class="card">
        <div class="card-num">03</div>
        <div class="card-label">Embed in ClickUp docs</div>
      </div>
    </div>
  </div>

  <div class="section">
    <p class="section-label">Setup</p>
    <h2 class="section-heading">Full workflow confirmed</h2>
    <p class="section-body">Claude → HTML file → Files app → GitHub → GitHub Pages → ClickUp. The whole chain is working end to end.</p>
  </div>
</main>

<footer>Devin Triana · Confidential</footer>
```

  </div>

  <script>
    const PASSWORD = 'dtrianaclickup';

    document.getElementById('pw').addEventListener('keydown', e => {
      if (e.key === 'Enter') checkPassword();
    });

    function checkPassword() {
      const val = document.getElementById('pw').value;
      if (val === PASSWORD) {
        document.getElementById('lock').classList.add('hidden');
        setTimeout(() => {
          document.getElementById('content').classList.add('visible');
        }, 600);
      } else {
        document.getElementById('err').textContent = 'Incorrect password — try again.';
        document.getElementById('pw').value = '';
      }
    }
  </script>

</body>
</html>
